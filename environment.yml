name: webrtc
channels:
    - defaults
dependencies:
    - python=3
    - python-dotenv
    - pip:
          - aiortc
          - python-socketio[asyncio_client]
          - python-socketio[client]
          - asyncio
